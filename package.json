{
  "name": "shadstrap",
  "version": "1.0.0",
  "description": "UI Components based off those provided by Bootstrap framework. Shadow DOM compatible.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cat src/utils.js src/dropdown.js src/index.js > dist/combined.js && npx babel dist/combined.js --out-file dist/index.js && rm dist/combined.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.26.4",
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.6.12"
  }
}
